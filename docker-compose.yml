version: "3"
services:
  wwwmama:
    image: nginx
    restart: always
    expose:
      - "80"
    volumes:
      - ./:/usr/share/nginx/html:ro
    environment:
      - VIRTUAL_HOST=mama.roseline.pe,www.mama.roseline.pe
      - LETSENCRYPT_HOST=mama.roseline.pe,www.mama.roseline.pe
      - LETSENCRYPT_EMAIL=miguekos1233@gmail.com
networks:
  default:
    external:
      name: nginx-proxy_default
